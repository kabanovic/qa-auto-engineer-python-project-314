[project]
name = "hexlet-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=9.1.1",
    "selenium>=4.46.0",
    "webdriver-manager>=4.1.2",
]

[dependency-groups]
dev = [
    "ruff>=0.16.3",
]

[tool.pytest.ini_options]
markers = [
    "smoke: быстрые тесты критического функционала (smoke-тесты)"
]
